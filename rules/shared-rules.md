# Shared rules

Starter template — edit this to match how the two of us actually want Claude
to behave. Delete sections that don't apply, add ones that do.

## Git / commits

- Write commit messages that explain *why*, not just what changed.
- Don't force-push shared branches without asking.
- Don't commit directly to `main` on shared projects — use a branch + PR.

## Code style

- (fill in: preferred formatting, linting, naming conventions)

## Communication

- When something is ambiguous, ask rather than guessing on anything hard to
  reverse (deploys, deleting data, pushing to shared branches).
- Keep explanations short by default; expand only when asked.

## Security

- Never commit real secrets outside `credentials/` in this repo.
- Never paste contents of `credentials/` into a public place.
